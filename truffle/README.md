# Crypto Bulls&Cows Smart Contracts

#### Compile contracts

`npm run compile`

#### Run tests

`npm run test`

#### Run Solidity code coverage

`npm run coverage`

#### Run all the migrations scripts

`npm run deploy:network`, `network` is `development`
